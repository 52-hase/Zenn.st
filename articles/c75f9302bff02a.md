---
title: "Ruby on RailsのアプリにLINEを組み込む"
emoji: "🦔"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: [LINE]
published: false
---
### 開発環境
- macOS
- VSCode
- Rails 7.1.3.3
- ruby-3.2.3


# LINEログインとは
・LINEアカウントを使ったソーシャルログインサービス
・自身のLINEアカウントの情報でログインすることができる
・ユーザーが新たにID・パスワードを作成する必要がない


# LINE Developersの登録

・LINE Developersコンソールの概要
https://developers.line.biz/ja/docs/line-developers-console/overview/

・こちらのリンクから登録する
https://developers.line.biz/console/

1. LINE Developers Console 
・アカウントを作成
・メールアドレスで登録→指定のアドレスにリンクを送信する

[![Image from Gyazo](https://i.gyazo.com/3fed27515776417c7393cbc04ba46524.png)](https://gyazo.com/3fed27515776417c7393cbc04ba46524)
・登録

チャネル作成
`Channel ID` と `Channel Secret` を控える

メールアドレス取得したい場合 → 権限を ON

`Callback URL` を設定
https://<your-domain>/users/auth/line/callback








# RailsでLINEログインを実装する手順


1. LINEログインチャネルを作成
2. コントローラ実装
3. ルーティング設定
4. ビューの実装（ログインボタン）
5. HTTPS化（ngrok）
6. LINE Developers設定

## 前提

- Rails（7系想定）
- HTTP 通信に typhoeus を使用
- 簡易ログインとして session[:user_id] を使用

## 1. LINEログインチャネルの作成

1. LINE Developers にアクセス  
   https://developers.line.biz/ja/

2. 「プロバイダ」→「LINEログインチャネル」を作成

3. チャネルID と チャネルシークレット をメモ  
   （Rails では環境変数で管理するのがベスト）

4. コールバックURLは後ほど設定（ngrok 必須）

## 2. コントローラの作成
```bash
$ bundle exec rails g controller LineLoginApi
```

## 3. LineLoginApi コントローラ実装

- **login**: LINEの認可画面へリダイレクトする
- **callback**: 認可コードを受け取ってログイン処理を行う
- **get_line_user_id_token**: 認可コードからアクセストークン（IDトークン）を取得
- **get_line_user_id**: IDトークンからユーザーID（sub）を取得

### app/controllers/line_login_api_controller.rb
```ruby:app/controllers/line_login_api_controller.rb
class LineLoginApiController < ApplicationController
  # 外部ライブラリの読み込み
  require 'json'        # JSON形式を扱うため
  require 'typhoeus'    # HTTPリクエストを送るためのライブラリ
  require 'securerandom' # ランダム文字列生成（CSRF対策）

  # LINEログインの認証画面にリダイレクトする処理
  def login
    # --- CSRF対策 ---
    # LINEログイン時は state を付与して、ログイン後に一致するか確認する
    session[:state] = SecureRandom.urlsafe_base64

    # LINEの認可エンドポイント
    base_authorization_url = 'https://access.line.me/oauth2/v2.1/authorize'

    # OAuth2 の認可コードフロー
    response_type = 'code'

    # LINEログインのクライアントID（環境変数で管理）
    client_id = ENV['LINE_LOGIN_CLIENT_ID']

    # コールバックURL（エンコード必須）
    redirect_uri = CGI.escape(line_login_api_callback_url)

    # CSRF対策で保存した state
    state = session[:state]

    # 要求する権限（profile + openid）
    scope = 'profile%20openid'

    # 最終的に生成される LINE のログインURL
    authorization_url =
      "#{base_authorization_url}?response_type=#{response_type}&client_id=#{client_id}&redirect_uri=#{redirect_uri}&state=#{state}&scope=#{scope}"

    # LINE のログイン画面へリダイレクト
    redirect_to authorization_url, allow_other_host: true
  end

  # LINEログイン後に返ってくるコールバック処理
  def callback
    # --- CSRFチェック ---
    # LINEログイン前に保存した state と一致するか確認
    if params[:state] == session[:state]

      # 認可コード（code）を使って LINEユーザーID を取得
      line_user_id = get_line_user_id(params[:code])

      # 既存ユーザーを検索。いなければ新規作成
      user = User.find_or_initialize_by(line_user_id: line_user_id)

      # 保存成功時
      if user.save
        # ログイン状態を保持
        session[:user_id] = user.id
        redirect_to after_login_path, notice: 'ログインしました'
      else
        redirect_to root_path, notice: 'ログインに失敗しました'
      end

    else
      # state 不一致 → CSRFの可能性
      redirect_to root_path, notice: '不正なアクセスです'
    end
  end

  private

  # code から LINEユーザーの一意ID（sub）を取得するメソッド
  def get_line_user_id(code)
    # id_token をまず取得する
    id_token = get_line_user_id_token(code)
    return nil if id_token.blank?

    # id_token を検証する LINE API
    url = 'https://api.line.me/oauth2/v2.1/verify'

    # 検証リクエスト
    options = {
      body: {
        id_token: id_token,
        client_id: ENV['LINE_LOGIN_CLIENT_ID'] # 自アプリのClient ID
      }
    }

    # POSTリクエスト送信
    response = Typhoeus::Request.post(url, options)
    return nil unless response.code == 200

    # sub = LINEユーザーの識別ID（ユニーク）
    JSON.parse(response.body)['sub']
  end

  # 認可コード（code）から id_token を取得するメソッド
  def get_line_user_id_token(code)
    # トークンを取得するためのエンドポイント
    url = 'https://api.line.me/oauth2/v2.1/token'

    # callback URL
    redirect_uri = line_login_api_callback_url

    # POST用パラメータ
    options = {
      headers: { 'Content-Type' => 'application/x-www-form-urlencoded' },
      body: {
        grant_type: 'authorization_code',   # 認可コードからトークンを取得
        code: code,                         # LINEから返却された認可コード
        redirect_uri: redirect_uri,         # 事前登録したコールバックURL
        client_id: ENV['LINE_LOGIN_CLIENT_ID'],         # Client ID
        client_secret: ENV['LINE_LOGIN_CLIENT_SECRET']  # Client Secret
      }
    }

    # リクエスト送信
    response = Typhoeus::Request.post(url, options)
    return nil unless response.code == 200

    # レスポンスの中に JWT形式の id_token が含まれる
    JSON.parse(response.body)['id_token']
  end
end

```

## 4. ルーティング設定
```ruby:config/routes.rb
# config/routes.rb
Rails.application.routes.draw do
  get 'line_login_api/login',    to: 'line_login_api#login'
  get 'line_login_api/callback', to: 'line_login_api#callback'
end
```

## 5. typhoeus のインストール
```ruby:Gemfile
# Gemfile
gem 'typhoeus'
```
```bash
$ bundle install
```

## 6. ビューにログインボタンを追加
```erb
<!-- app/views/static_pages/before_login.html.erb -->
<div>
  <%= link_to "LINEでログイン", line_login_api_login_path,
      class: "rounded-lg py-3 px-5 bg-green-500 text-white inline-block font-medium" %>
</div>
```

## 7. ログイン後のページでID確認（任意）
```erb
<!-- app/views/static_pages/after_login.html.erb -->
<div>
  <% current_user = User.find(session[:user_id]) %>
  <%= "現在ログイン中のユーザーID：#{current_user.id}" %>
</div>
```

## 8. ngrok を使って HTTPS 化する

LINEログインは http では動作しないため、ngrok を使用してローカル Rails を https 公開します。

### ngrok のセットアップ
```bash
$ unzip ~/Downloads/ngrok-v3-stable-darwin-amd64.zip
$ ./ngrok config add-authtoken <your-token>
```

### ngrok の起動
```bash
$ ./ngrok http 3000
```

すると以下のような URL が表示されます：
```
https://xxxxx.jp.ngrok.io
```

## 9. Rails にホストを許可する
```ruby
# config/environments/development.rb
config.hosts << '.jp.ngrok.io'
```

## 10. LINE Developers にコールバックURLを登録
```
https://xxxxx.jp.ngrok.io/line_login_api/callback
```

## 11. 動作確認

1. ngrok を起動したまま Rails を `./bin/dev` で起動
2. `https://xxxxx.jp.ngrok.io` にアクセス
3. 「LINEでログイン」を押す
4. LINE の認証画面が出る
5. ログイン後のページに戻れば成功
