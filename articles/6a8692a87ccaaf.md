---
title: "チャットの表示がユーザーごと左右に分かれるようにする"
emoji: "🌊"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: []
published: false
---



・仕事で活かせる学習環境だったことの記載
・ドメインの支払いについての確認（更新済）
（AWS、閲覧数のサービス、スポティファイなど）
・デプロイしたらエラーや挙動の確認をする



app/views/messages/_message.html.erb

```jsx
<li class="list-group-item bg-transparent border-0" data-user-id="<%= message.user_id %>">
  <div class="card bg-transparent border-0">
    <div class="card-body">
      <div class="d-flex align-items-start message-row">
        <% if message.user.avatar.attached? %>
          <img class="rounded-circle img-thumbnail" src="<%= url_for(message.user.avatar) %>" style="width: 50px; height: 50px; object-fit: cover;">
        <% else %>
          <div class="bg-secondary text-white rounded-circle d-inline-flex justify-content-center align-items-center" style="width: 50px; height: 50px; font-size: 20px;">
            <%= message.user.name[0].upcase %>
          </div>
        <% end %>

        <div style="max-width: 50%;">
          <div class="text-white">
            <%= message.user.name %>
            <span style="color: rgba(108, 117, 125, 10); margin-left: 0.5rem;"><%= message.created_at.strftime("%Y-%m-%d %H:%M") %></span>
          </div>
          <div class="message-bubble mt-2">
            <div class="card-text" style="color: black; white-space: pre-wrap;"><%= message.content %></div>
            <% if message.image.attached? %>
              <div class="mt-2">
                <img class="img-fluid" src="<%= url_for(message.image) %>" style="width: 300px; height: auto;">
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</li>
```

app/views/live_rooms/show.html.erb

```jsx
<div class="container mt-5 d-flex flex-column align-items-center live-room-container">
  <h2 class="text-center mb-4 live-room-title">
    <%= @live_room.name %>
  </h2>
  <div id="messages" class="messages-container">
    <ul class="list-group">
      <%= render @messages %>
    </ul>
  </div>
</div>

<div id="flash-messages" style="position: fixed; top: 3px; left: 50%; transform: translateX(-50%); z-index: 1050; width: 100%; max-width: 600px;">
  <% if flash[:notice] %>
    <div class="alert alert-success text-center" style="background-color: #2c3e50; color: #ecf0f1; padding: 15px; border-radius: 5px;">
      <%= flash[:notice] %>
    </div>
  <% end %>
</div>

<% if user_signed_in? %>
  <div id="user-id" data-user-id="<%= current_user.id %>" style="display: none;"></div>

  <div class="fixed-bottom bg-light py-3">
    <div class="container">
      <div class="form-group d-flex">
        <textarea type="text" class="form-control" id="chat-input" placeholder="メッセージを入力" style="border-radius: 20px 0 0 20px;"></textarea>
        <label for="chat-image" id="chat-image-label" class="btn btn-outline-secondary">画像</label>
        <input type="file" id="chat-image" class="form-control-file d-none" />
        <button id="button" class="btn btn-outline-primary">送信</button>
        <!-- ライブルームの名前とURLをデータ属性で渡す -->
        <div id="share-button" data-room-name="<%= @live_room.name %>" data-room-url="<%= live_room_path(@live_room) %>">
          <button id="share-to-x">
            <%= image_tag 'logo-black.png', alt: 'X Logo', style: 'width: 15px; height: 15px;' %>
            でライブをシェアする
          </button>
        </div>
      </div>
    </div>
  </div>
<% end %>

<% content_for :meta_tags do %>
  <meta name="live_room_id" content="<%= @live_room.id %>">
<% end %>

```

app/channels/live_room_channel.rb

```jsx
class LiveRoomChannel < ApplicationCable::Channel
  def subscribed
    live_room = LiveRoom.find(params[:live_room_id])
    stream_for live_room
  end

  def speak(data)
    live_room = LiveRoom.find_by(id: data["live_room_id"])
    image_data = data["image"]
    message_content = data["message"].presence || ""

    message = live_room.messages.new(content: message_content, user_id: data["user_id"])

    if image_data.present?
      decoded_image = Base64.decode64(image_data)
      message.image.attach(io: StringIO.new(decoded_image), filename: "uploaded_image.png")
    end

    message.save!

    # 左右の表示判定はクライアント側(JS)で行うため、current_user依存の偽装ログインは不要
    template = ApplicationController.renderer.render(
      partial: "messages/message",
      locals: { message: message }
    )
    LiveRoomChannel.broadcast_to(live_room, template)
  end
end
```

app/javascript/channels/live_room_channel.js

```jsx
import consumer from "./consumer";

// URLからlive_room_idを取得
function getLiveRoomIdFromUrl() {
  const url = window.location.pathname;
  const match = url.match(/live_rooms\/(\d+)/);
  return match ? match[1] : null;
}

// ログインユーザーIDを取得
function getUserId() {
  const userIdElement = document.getElementById("user-id");
  return userIdElement ? userIdElement.dataset.userId : null;
}

// メッセージの左右表示を設定
function applyMessagePosition(li) {
  const row = li.querySelector(".message-row");

  if (!row) return;

  if (String(li.dataset.userId) === String(userId)) {
    row.classList.add("justify-content-end");
  } else {
    row.classList.add("justify-content-start");
  }
}

const liveRoomId = getLiveRoomIdFromUrl();
const userId = getUserId();
const App = {};

// LiveRoomChannelに接続
if (liveRoomId) {
  App.room = consumer.subscriptions.create(
    { channel: "LiveRoomChannel", live_room_id: liveRoomId },
    {
      // 接続成功時
      connected() {
        console.log("Connected to LiveRoomChannel");
      },

      // 接続切断時
      disconnected() {
        console.log("Disconnected from LiveRoomChannel");
      },

      // メッセージを受信
      received(message) {
        const messages = document.getElementById("messages");

        if (messages) {
          const ul = messages.querySelector("ul");

          if (ul) {
            // 受け取ったHTMLを追加
            ul.insertAdjacentHTML("beforeend", message);

            // 追加したメッセージの左右を判定
            const lastLi = ul.lastElementChild;

            if (lastLi) {
              applyMessagePosition(lastLi);
            }
          }
        }
      },

      // メッセージを送信
      speak(content, image = null) {
        return this.perform("speak", {
          message: content,
          image: image,
          live_room_id: liveRoomId,
          user_id: userId
        });
      }
    }
  );

  // HTMLの読み込み完了後に処理
  document.addEventListener("DOMContentLoaded", function () {

    // 初期表示されているメッセージの左右を判定
    document
      .querySelectorAll("#messages li[data-user-id]")
      .forEach(applyMessagePosition);

    const input = document.getElementById("chat-input");
    const imageInput = document.getElementById("chat-image");
    const button = document.getElementById("button");
    const chatImageLabel = document.getElementById("chat-image-label");

    // 画像選択時にラベルを更新
    imageInput.addEventListener("change", function () {
      if (imageInput.files.length > 0) {
        chatImageLabel.textContent = "画像が添付されています";
      }
    });

    // 送信ボタンをクリックしたとき
    button.addEventListener("click", function () {
      const content = input.value;
      const file = imageInput.files[0];

      if (file) {
        // 画像をBase64形式に変換して送信
        const reader = new FileReader();

        reader.onload = function (event) {
          const base64String = event.target.result.split(",")[1];
          App.room.speak(content, base64String);
        };

        reader.readAsDataURL(file);
      } else {
        // テキストのみ送信
        App.room.speak(content);
      }

      // 入力欄と画像をリセット
      input.value = "";
      imageInput.value = "";
      chatImageLabel.textContent = "画像";
    });
  });
}
```




```
import consumer from "./consumer";

// URLからlive_room_idを取得
function getLiveRoomIdFromUrl() {
  const url = window.location.pathname;
  const match = url.match(/live_rooms\/(\d+)/);
  return match ? match[1] : null;
}

// ログインユーザーIDを取得
function getUserId() {
  const userIdElement = document.getElementById("user-id");
  return userIdElement ? userIdElement.dataset.userId : null;
}

// メッセージの左右表示を設定
function applyMessagePosition(li) {
  const row = li.querySelector(".message-row");

  if (!row) return;

  // メッセージ表示時にログインユーザーIDを取得
  const currentUserId = getUserId();

  if (String(li.dataset.userId) === String(currentUserId)) {
    row.classList.add("justify-content-end");
  } else {
    row.classList.add("justify-content-start");
  }
}

const liveRoomId = getLiveRoomIdFromUrl();
const App = {};

if (liveRoomId) {
  App.room = consumer.subscriptions.create(
    {
      channel: "LiveRoomChannel",
      live_room_id: liveRoomId
    },

    {
      connected() {
        console.log("Connected to LiveRoomChannel");
      },

      disconnected() {
        console.log("Disconnected from LiveRoomChannel");
      },

      received(message) {
        const messages = document.getElementById("messages");

        if (messages) {
          const ul = messages.querySelector("ul");

          if (ul) {
            // 受け取ったメッセージを追加
            ul.insertAdjacentHTML("beforeend", message);

            // 追加したメッセージの左右を設定
            const lastLi = ul.lastElementChild;

            if (lastLi) {
              applyMessagePosition(lastLi);
            }
          }
        }
      },

      speak(content, image = null) {
        // 送信時にログインユーザーIDを取得
        const userId = getUserId();

        return this.perform("speak", {
          message: content,
          image: image,
          live_room_id: liveRoomId,
          user_id: userId
        });
      }
    }
  );

  document.addEventListener("DOMContentLoaded", function () {
    // 初期表示されているメッセージの左右を設定
    document
      .querySelectorAll("#messages li[data-user-id]")
      .forEach(applyMessagePosition);

    const input = document.getElementById("chat-input");
    const imageInput = document.getElementById("chat-image");
    const button = document.getElementById("button");
    const chatImageLabel = document.getElementById("chat-image-label");

    // 画像が選択されたときにラベルを更新
    imageInput.addEventListener("change", function () {
      if (imageInput.files.length > 0) {
        chatImageLabel.textContent = "画像が添付されています";
      }
    });

    // 送信ボタン
    button.addEventListener("click", function () {
      const content = input.value;
      const file = imageInput.files[0];

      if (file) {
        // 画像をBase64に変換して送信
        const reader = new FileReader();

        reader.onload = function (event) {
          const base64String = event.target.result.split(",")[1];
          App.room.speak(content, base64String);
        };

        reader.readAsDataURL(file);
      } else {
        // テキストだけ送信
        App.room.speak(content);
      }

      // 入力欄をリセット
      input.value = "";
      imageInput.value = "";
      chatImageLabel.textContent = "画像";
    });
  });
}





⭕️やること
・一旦作った諸々を見直して修正、
・SVC と JavaScriptについては明確にしておく
・アプリのコードをGitHubごと覚えさせてファイルごと整理して、自分の頭も整理していく
・話すことおさらい、
・会社についてのおさらい、チャッピーにどんなアプリや実績なのかを聞いてみる
などしていく






# 自己アピールの全文
受講していたプログラミングスクール「RUNTEQ」では、RubyとRuby on Railsの基礎からフレームワークを用いた実践的なWebアプリ開発と、Webアプリをデプロイするまでのプロセスを学びました。
具体的には、README作成による要件・仕様の整理、画面遷移図やER図による設計、issueベースでのタスク管理を通じた開発、そしてMVPとしてのリリースという流れです。これらの学びを生かし、音楽ライブ・フェスの感想をリアルタイムで共有できるチャットアプリ「LiveFes」を開発し、要件定義から設計、実装、デプロイまでを一貫して経験いたしました。
学習で身につけたプログラミングの吸収方法やアプリ作成のプロセスを活かしながら、前職で培った継続力を発揮し、実務の中で日々知識を深め、成長してまいります。



### 面接・応募前に聞くべき質問
・静岡市葵区にある勤務先の住所を教えてもらえますか？
・入社後はDef tribeと直接雇用契約を結ぶのでしょうか？
・Rubyの研修はどのくらいの期間ありますか？
・研修後、実際にRuby on Railsの開発案件に配属される可能性はありますか？
・静岡での勤務は自社開発ですか？ それとも客先常駐ですか？



### メモ
面接回答集｜一問一答
① 自己紹介をお願いします

Q. 自己紹介をお願いします。

A.
長谷川哲也と申します。
大学卒業後、日本郵便株式会社に入社し、窓口社員として郵便・貯金・保険を担当していました。
2023年12月に退社後、プログラミングスクールのRUNTEQに入学し、現在までRuby on Railsを中心に学習しています。
本日はよろしくお願いいたします。

② なぜエンジニアを目指したのですか？

Q. エンジニアを目指したきっかけを教えてください。

A.
兄がエンジニアをしており、仕事の話を聞く中で、日々学習してスキルを伸ばせる仕事に興味を持ったことがきっかけです。
その後RUNTEQで実際にプログラミングを学び、アプリの仕組みを知り、実際に作ることに魅力を感じました。
サービスを使う側から、作る側になりたいと思い、エンジニアを目指しました。

③ なぜ日本郵便を退職したのですか？

Q. 前職の退職理由を教えてください。

A.
郵便局で働く中で、業界の変化や将来のキャリアについて考えるようになったことがきっかけです。
社会の変化により積極的に対応できる環境で、自分自身も成長していきたいと考え、エンジニアへの転職を決意しました。

④ なぜ郵便局に入社したのですか？

Q. 1社目に日本郵便を選んだ理由は？

A.
人とコミュニケーションを取れることと、地域に根差した仕事ができることの2点から郵便局を選びました。
お客様と直接関わりながら、地域の方々の生活を支えられる点に魅力を感じました。

⑤ 前職で頑張ったことは？

Q. これまでの仕事で頑張ったことを教えてください。

A.
郵便物販の販売促進に取り組み、所属していた局が約50局ある連絡会の中で営業成績1位を達成しました。
業務の効率化について提案し、チームで協力して営業活動の時間を作ったことで、販売促進につなげました。

⑥ 仕事で大変だったことは？

Q. 前職で大変だったことを教えてください。

A.
営業の際に、商品のメリット・デメリットを正しく伝え、お客様に十分理解していただくことです。
そのため、日々の会話やスムーズな手続きなどを通して信頼関係を築くことを意識していました。

⑦ あなたの強みは？

Q. あなたの強みを教えてください。

A.
私の強みは継続力です。
前職では業務改正に対応するため継続的に学習していました。
現在のプログラミング学習でも、毎日Notionに学んだことを記録し、内容をZennの記事として発信しています。
継続して学習する力を、実務でも活かしたいと考えています。

⑧ 弱みは？

Q. あなたの弱みを教えてください。

A.
下調べに時間をかけすぎてしまうところです。
以前は実装方法に納得するまで調べ続けることがありました。
現在は、ある程度理解した段階でまず実装し、分からない部分は実装しながら調べるようにしています。

⑨ 企業選びで重視していることは？

Q. 他社も受けていると思いますが、企業選びの軸は何ですか？

A.
意見や考えを提案しやすい、オープンな環境を重視しています。
前職でも、先輩・後輩が意見を出し合うことでチームとして成長できることを実感しました。
そのため、積極的に意見交換できる環境で働きたいと考えています。

プログラミングについて
⑩ RUNTEQで何を学びましたか？

Q. RUNTEQでは何を学びましたか？

A.
RubyとRuby on Railsを中心に、Webアプリケーション開発について学びました。
要件整理、設計、実装、GitHubでのIssue管理、コードレビュー、デプロイまで、一連の開発プロセスを経験しました。

⑪ プログラミング学習で工夫したことは？

Q. プログラミング学習で工夫したことはありますか？

A.
毎日Notionに学んだことをアウトプットし、苦戦した実装は記事として発信することを意識しました。
アウトプットすることで理解を深め、アプリ開発ではIssueごとに計画的に実装を進められるようになりました。

⑫ プログラミング学習で大変だったことは？

Q. プログラミング学習で大変だったことは？

A.
分からないことをどこまで深く調べるべきか判断することです。
調べすぎると実装が進まなくなるため、今すぐ必要か判断できない情報はNotionに残し、実装後にまとめて調べるようにしています。

LiveFesについて
⑬ LiveFesとはどんなアプリですか？

Q. LiveFesについて簡単に説明してください。

A.
音楽ライブに参加した人たちが、ライブ後の余韻や感想を共有できるWebアプリです。
主な機能は、複数人で利用できるリアルタイムチャットと、ライブで披露された楽曲を確認・再生できる機能です。

⑭ なぜLiveFesを作ったのですか？

Q. LiveFesを作った理由は何ですか？

A.
私自身が音楽ライブに行くことが好きで、ライブ終了後に感想を共有する場所があれば面白いと考えたからです。
ライブ後の余韻や喪失感を参加者同士で共有できるサービスを作りたいと思い、開発しました。

⑮ LiveFesの技術構成を教えてください

Q. 使用した技術を教えてください。

A.
RubyとRuby on Railsを使用しています。
認証にはDevise、リアルタイムチャットにはRailsのAction CableとJavaScriptを使用しています。
画像保存にはAWSのS3、楽曲情報の取得にはSpotify Web APIを使用しています。

⑯ 一番力を入れた機能は？

Q. LiveFesで一番力を入れた機能は何ですか？

A.
リアルタイムチャット機能です。
RailsのAction Cableを使ってメッセージをリアルタイムに送受信し、JavaScriptでDOMを操作して画面に即座に反映する仕組みを実装しました。

⑰ 開発で苦労したことは？

Q. 開発で苦労したことを教えてください。

A.
チャット機能でJavaScriptのイベントが発火しない問題に苦労しました。
バックエンドとフロントエンドのどちらに原因があるのかを切り分け、デバッグやコードの確認を行いました。
最終的に講師にも相談し、Turbo Driveの影響が原因だと分かりました。

⑱ LiveFesはどのように開発しましたか？

Q. 開発の進め方を教えてください。

A.
まずREADMEで仕様を整理し、Figmaで画面遷移図、draw.ioでER図を作成しました。
その後GitHubでIssueを作成してタスク管理を行い、実装後はPull Requestを作成してレビューを行いました。
MVPリリース後はフィードバックをもとに改善しました。

⑲ デプロイには何を使いましたか？

Q. どのようにデプロイしましたか？

A.
Herokuを使用しました。
初めてのWebアプリ開発だったため、インフラ設定を複雑にせず、開発とデプロイに集中できる環境を選びました。
データベースにはPostgreSQLを使用しています。

技術について
⑳ 最近学んでいる技術は？

Q. 最近、新しく学んでいる技術はありますか？

A.
現在はPHPとLaravelを学習しています。
Rubyと構文が似ていることや、Webアプリケーション開発に使われていることに興味を持ち、簡単なアプリを作りながら学習しています。

㉑ 興味のある技術は？

Q. 今後学びたい技術はありますか？

A.
クラウド技術に興味があります。
LiveFesでAWSのS3を使用したことをきっかけに、AWSに興味を持ちました。
今後はEC2やLambdaなども学び、クラウドを実際の開発に活かせるようになりたいです。

㉒ 面白いと感じた技術は？

Q. 今まで触れた技術で面白かったものは？

A.
Dockerです。
仮想環境を構築することで、環境の違いを気にせず開発できる点が便利だと感じました。
開発環境を再現しやすいところにも魅力を感じています。

㉓ チーム開発の経験は？

Q. チーム開発の経験はありますか？

A.
実際の開発は個人での経験が中心です。
ただ、GitHubのIssue、ブランチ管理、Pull Requestなどを使って開発してきました。
これらの経験を活かし、チーム開発でもコミュニケーションを取りながら開発に貢献したいと考えています。

キャリア・入社について
㉔ 入社後どうなりたいですか？

Q. 入社後のキャリアについて教えてください。

A.
まず1年目は、技術の基礎と現場で必要なスキルを身につけ、積極的にキャッチアップしたいです。
3年目には得意な技術を伸ばし、技術面でもチームに貢献できるようになりたいです。
将来的には、チームをつなぎ、まとめられるエンジニアを目指しています。

㉕ 最後に何か伝えたいことはありますか？

Q. 最後にアピールしたいことはありますか？

A.
私は、継続して学習することを大切にしてきました。
RUNTEQでRuby on Railsを学び、実際にLiveFesを開発してリリースまで経験しました。
これまで培った継続力を活かして、入社後も新しい技術を積極的に学び、早く実務で貢献できるようになりたいと考えています。

⭐ 特に暗記しておきたい5つ

面接前に全部丸暗記するより、まずこの5つを自分の言葉で話せるようにするのがおすすめです。

自己紹介
なぜエンジニアなのか
退職理由
LiveFesについて
自分の強み

この5つがつながっていると、多少質問の聞き方が変わっても対応しやすいです。