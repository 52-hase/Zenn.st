---
title: "仮：アプリについて"
emoji: "🦁"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: [アプリ]
published: false
---
### 開発環境
- macOS
- VSCode
- Rails 7.1.3.3
- ruby-3.2.3
- PostgreSQL 16.2
- Heroku

### 行いたいこと
- 


<br>
<br>
<br>

# 雑メモ
・ルートの設定
・次はビューの記載する
・

docker compose exec web bash


1.setting
2.login-logout








# 
・Rails7では使えていた。
```
<%= javascript_importmap_tags %>
```
・Rails8の場合はこれにする
```
<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
```


```
<!DOCTYPE html>
<html>
  <head>
    <title>DEMO</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 dark:bg-gray-900 text-gray-800 dark:text-gray-200 min-h-screen flex flex-col">

    <!-- ヘッダー -->
    <header class="bg-white dark:bg-gray-800 shadow-md fixed top-0 left-0 right-0 z-50">
      <nav class="container mx-auto flex justify-between items-center p-4">
        <h1 class="text-xl font-bold text-gray-800 dark:text-white">
          <%= link_to "Demoアプリ", root_path, class: "hover:text-blue-500 transition" %>
        </h1>

        <ul class="flex space-x-4 text-gray-700 dark:text-gray-300">
          <% if user_signed_in? %>
            <li>
              <%= link_to "ログアウト", destroy_user_session_path,
    method: :delete,
    class: "px-3 py-2 rounded-md hover:bg-gray-100 dark:hover:bg-gray-700 transition" %>

            </li>
          <% else %>
            <li>
              <%= link_to "新規登録", new_user_registration_path,
                  class: "px-3 py-2 rounded-md hover:bg-gray-100 dark:hover:bg-gray-700 transition" %>
            </li>
            <li>
              <%= link_to "ログイン", new_user_session_path,
                  class: "px-3 py-2 rounded-md bg-blue-500 text-white hover:bg-blue-600 transition" %>
            </li>
          <% end %>
        </ul>
      </nav>
    </header>

    <!-- メインコンテンツ -->
    <main class="flex-grow container mx-auto p-6 pt-24">
      <%= yield %>
    </main>

    <!-- フッター -->
    <footer class="bg-white dark:bg-gray-800 border-t mt-10 shadow-inner">
      <div class="container mx-auto text-center py-4 text-sm text-gray-500 dark:text-gray-400">
        <p>© <%= Time.current.year %> Demo App</p>
        <div class="space-x-4 mt-2">
          <%= link_to "利用規約", "#", class: "hover:text-blue-500 transition" %>
          <%= link_to "プライバシー", "#", class: "hover:text-blue-500 transition" %>
        </div>
      </div>
    </footer>

  </body>
</html>

```
修正のページ




```
// app/javascript/application.js
import "@hotwired/turbo-rails"
import Rails from "@rails/ujs"  // ← UJS を読み込む

Rails.start()  // ← method: :delete を有効化
import "controllers" // Stimulus等を使用する場合

```

```
Rails.application.routes.draw do
  # Devise（ユーザー認証）
  devise_for :users, controllers: {
    registrations: 'users/registrations',
    sessions: 'users/sessions'
  }

  # 投稿機能
  resources :posts

  # 静的ページ
  root "static_pages#home"
  get "static_pages/home"
  get "static_pages/help"
  get "static_pages/about"
end

```



<br>
<br>


### 以上です。

<br>
<br>
<br>