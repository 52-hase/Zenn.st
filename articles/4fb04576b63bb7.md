---
title: "アプリのファイル諸々"
emoji: "🍣"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: [controller]
published: false
---



## コンローラー
::::details　コントローラー


### app/controllers/application_controller.rb

```rb

# ApplicationControllerは、アプリ内のすべてのコントローラーの親クラスです。
# ここに書かれたルールやメソッドは、他のすべてのコントローラーで共通して利用できます。
class ApplicationController < ActionController::Base
  # CSRF（悪意のある第三者によるなりすまし操作）対策を有効にします。
  # フォーム送信などに不正なトークンが含まれていた場合、例外（エラー）を発生させます。
  protect_from_forgery with: :exception

  # Devise（認証機能）のコントローラーが動く時だけ、アクションの前に「configure_permitted_parameters」を実行します。
  before_action :configure_permitted_parameters, if: :devise_controller?

  # Deviseで許可するパラメーター（入力項目）を定義するメソッド
  def configure_permitted_parameters
    # ユーザー登録(sign_up)の際に、デフォルトの項目以外に「name」という項目の保存も許可します。
    devise_parameter_sanitizer.permit(:sign_up, keys: [ :name ])
  end

  # 特定のユーザーが「ログインしている状態」をシミュレートしてHTMLを生成するためのクラスメソッドです。
  # 主にコントローラーの外部（バックグラウンド処理など）からビューをレンダリングしたい時に使用されます。
  def self.render_with_signed_in_user(user, *args)
    # Warden（Deviseの基盤システム）の設定を読み込み、キーの整合性を取ります。
    ActionController::Renderer::RACK_KEY_TRANSLATION["warden"] ||= "warden"
    
    # 指定されたユーザーを「ログイン済み」として扱うWardenプロキシ（身代わり）を作成します。
    proxy = Warden::Proxy.new({}, Warden::Manager.new({})).tap { |i| i.set_user(user, scope: :user) }
    
    # 作成したプロキシ（ログイン状態）をレンダラーに渡し、その状態でレンダリング（描画）を実行します。
    renderer = self.renderer.new("warden" => proxy)
    renderer.render(*args)
  end
end
```


### app/controllers/live_events_controller.rb
```rb
# ApplicationController（共通ルールを管理する親クラス）を継承しています。
class LiveEventsController < ApplicationController
  
  # ライブイベントの一覧を表示するためのアクションです。
  # ブラウザで /live_events にアクセスした際に実行されます。
  def index
    # データベースから全てのライブイベントのデータを取り出し、
    # インスタンス変数（@live_events）に代入してビューに渡します。
    @live_events = LiveEvent.all
  end

  # 特定のライブイベントの情報を詳細に表示するためのアクションです。
  # ブラウザで /live_events/1 のようにIDを指定してアクセスした際に実行されます。
  def show
    # URLに含まれるID（params[:id]）を元に、
    # データベースから該当する1件のライブイベントを検索して取得します。
    @live_event = LiveEvent.find(params[:id])
  end
end
```


### app/controllers/live_rooms_controller.rb
```rb
# ApplicationController（共通設定）を継承した、ルーム管理用コントローラー
class LiveRoomsController < ApplicationController
  
  # ルームの一覧を表示するためのアクション
  def index
    # 全てのルームデータを取得して、一覧画面（index.html.erb）へ渡します
    @live_rooms = LiveRoom.all
  end

  # 特定のルームを表示するためのアクション
  def show
    # URLのID（例: /live_rooms/5 なら 5）を元に、該当するルームを1件取得
    @live_room = LiveRoom.find(params[:id])
    
    # そのルームに紐づくメッセージを取得します
    # .includes(:user) : 「N+1問題」を避けるため、投稿したユーザー情報もまとめて取得（高速化）
    # .order(created_at: :asc) : メッセージを古い順（作成日時が昇順）に並べて表示
    @messages = @live_room.messages.includes(:user).order(created_at: :asc)
  end

  # 新しいルームを保存するためのアクション
  def create
    # 下のprivateにある「live_room_params」を使って安全にデータを受け取ります
    @live_room = LiveRoom.new(live_room_params)
    
    if @live_room.save
      # 保存に成功した場合、メッセージを表示してそのルームの個別ページへ移動
      flash[:notice] = "ルームが作成されました。"
      redirect_to @live_room
    else
      # 保存に失敗（名前が空欄など）した場合、新規作成画面（new）を再表示します
      render :new
    end
  end

  private

  # 【ストロングパラメーター】
  # セキュリティのため、Webから送られてきたデータのうち「name」だけを受け取るよう制限します
  def live_room_params
    params.require(:live_room).permit(:name)
  end
end
```


### app/controllers/musics_controller.rb
```rb
# ApplicationController（共通設定）を継承した、音楽（楽曲）管理用コントローラー
class MusicsController < ApplicationController
  
  # 楽曲一覧を取得するためのアクション
  # アーティスト別、または「特定のアーティストの特定のライブ」別に曲を表示します
  def index
    # URLパラメーターからアーティスト情報を取得します
    @artist = Artist.find(params[:artist_id])
    
    # もしURLに live_event_id が含まれていれば、そのライブイベント情報を取得します
    @live_event = LiveEvent.find(params[:live_event_id]) if params[:live_event_id].present?

    # 【取得条件の分岐】
    if @live_event
      # ライブイベントが指定されている場合：そのアーティストの曲の中から、そのライブに関連するものだけを抽出
      @musics = @artist.musics.where(live_event_id: @live_event.id)
    else
      # ライブイベントの指定がない場合：そのアーティストの曲を全て取得
      @musics = @artist.musics
    end

    # 【データの整形】
    # 取得した曲データを、ViewやAPIで扱いやすいようにハッシュ形式の配列に変換します
    @musics = @musics.map do |music|
      {
        name: music.name,              # 曲名
        artist: music.artist_name,      # アーティスト名
        id: music.spotify_track_id,    # Spotifyの楽曲識別ID
        live_event_id: music.live_event_id # 関連するライブイベントのID
      }
    end
  end

  # 特定の楽曲の詳細を取得するためのアクション
  def show
    # 親となるアーティストを取得
    @artist = Artist.find(params[:artist_id])
    
    # 通常のIDではなく、SpotifyのトラックID（spotify_track_id）を使って楽曲を1件検索します
    @music = Music.find_by(spotify_track_id: params[:id])
  end
end
```

### app/controllers/users_controller.rb
```rb
class UsersController < ApplicationController
  # 指定したIDのユーザー情報を取得して詳細画面を表示するアクション
  def show
    # URLのパスから取得したID（params[:id]）を元に、データベースからユーザーを探します。
    @user = User.find(params[:id])
  end

  # ユーザー情報を更新するアクション
  def update
    # 更新対象のユーザーをデータベースから特定します。
    @user = User.find(params[:id])

    # チェックボックスなどで「アバターを削除（remove_avatar）」が選択されている場合の処理
    if params[:remove_avatar] == "on"
      # アバター画像が添付されている場合、Active Storageのファイルを物理削除します。
      @user.avatar.purge if @user.avatar.attached?
    end

    # フォームから送られてきたパラメーター（user_params）を使って更新を試みます。
    if @user.update(user_params)
      # 更新に成功した場合は、フラッシュメッセージをセットして詳細画面へリダイレクトします。
      flash[:notice] = "プロフィールが更新されました。"
      redirect_to @user
    else
      # バリデーションエラーなどで更新に失敗した場合は、showテンプレートを再描画（レンダリング）します。
      # ※ redirect_to ではないため、入力内容は保持されたままエラーメッセージが表示されます。
      render "show"
    end
  end

  private

  # ストロングパラメーター：外部からのリクエストのうち、許可する項目を制限します（セキュリティ対策）。
  def user_params
    # userキーの中にある name, email, avatar のみの変更を許可します。
    params.require(:user).permit(:name, :email, :avatar)
  end
end
```

::::



::::details Gemfile

```Gemfile
source "https://rubygems.org"

ruby "3.2.3"

# --- Rails 根幹・標準機能 ---
gem "rails", "~> 7.1.3", ">= 7.1.3.3"
gem "sprockets-rails"         # アセットパイプライン（CSS/JSの配信）を管理
gem "pg", "~> 1.2"            # PostgreSQLデータベースに接続するためのアダプタ
gem "puma", ">= 5.0"          # Rails標準のWebサーバー
gem "jsbundling-rails"        # JavaScriptのビルドツール（esbuild等）との連携
gem "turbo-rails"             # 画面遷移を高速化するHotwireの中心的機能
gem "stimulus-rails"          # JavaScriptで動きをつける軽量フレームワーク
gem "cssbundling-rails"       # CSSのビルドツール（Tailwind, Bootstrap等）との連携
gem "jbuilder"                # JSON形式でデータを返すためのテンプレートエンジン
gem "tzinfo-data", platforms: %i[ windows jruby ] # タイムゾーンデータをOS間で共有
gem "bootsnap", require: false # アプリの起動を高速化するためのキャッシュ機能

# --- 開発・テスト用ツール ---
group :development, :test do
  gem "debug", platforms: %i[ mri windows ] # ruby/debugによるデバッグ機能
end

group :development do
  gem "web-console" # ブラウザ上のエラー画面でコードを実行できるデバッグツール
end

group :test do
  gem "capybara"           # ブラウザ操作をシミュレートするテストツール
  gem "selenium-webdriver" # ブラウザを自動操作するためのドライバー
  gem "rspec-rails"        # Rubyで最も一般的なテストフレームワーク
  gem "factory_bot_rails"  # テスト用のダミーデータを簡単に作成するツール
end

# --- アプリケーション固有機能・ライブラリ ---

# 認証機能（ログイン、ログアウト、パスワード再設定など）
gem "devise"

# 開発環境で送信されたメールをブラウザ上で確認できる（本物のメールを送らない）
gem "letter_opener_web", "~> 3.0"

# .envファイルから環境変数（APIキー等）を読み込む
gem "dotenv-rails"

# CSSフレームワーク「Bootstrap 5」をRailsで利用可能にする
gem "bootstrap", "~> 5.3.3"

# 画像のリサイズやトリミングをサーバー側で行う（Active Storageで多用）
gem "image_processing", "~> 1.2"

# AWSのクラウドストレージ（S3）にファイルを保存するためのSDK
gem "aws-sdk-s3"

# Googleアカウントによるソーシャルログインを可能にする
gem "omniauth-google-oauth2"

# OAuth認証時の脆弱性（CSRF）を保護する
gem "omniauth-rails_csrf_protection"

# ページごとにタイトルや説明文を設定し、SEO（検索エンジン最適化）を強化する
gem "meta-tags"

# Spotify APIを簡単に扱うためのライブラリ（楽曲検索や再生情報取得）
gem "rspotify"

# Rails公式が推奨するコードスタイルのチェック（自動整形）ツール
gem "rubocop-rails-omakase", require: false, group: [ :development ]

# ソースコードをスキャンして脆弱性（セキュリティ問題）を見つける静的解析ツール
gem "brakeman", "~> 6.2.2"
```
::::







::::details Dockerfile
```Dockerfile
# syntax = docker/dockerfile:1

# 利用するRubyのバージョンを定義（引数として共通化）
ARG RUBY_VERSION=3.2.3
# ベースとなる軽量なRubyイメージ（スリム版）を指定し、'base'と命名
FROM registry.docker.com/library/ruby:$RUBY_VERSION-slim as base

# アプリケーションを配置する作業ディレクトリを設定
WORKDIR /rails

# 本番環境（production）用の環境変数をまとめて定義
ENV RAILS_ENV="production" \
    BUNDLE_DEPLOYMENT="1" \
    BUNDLE_PATH="/usr/local/bundle" \
    BUNDLE_WITHOUT="development" # 開発用Gemはインストールしない設定


# --- 【ステージ1：ビルド用】イメージサイズ削減のための使い捨てステージ ---
FROM base as build

# GemやNodeモジュールのビルドに必要なLinuxパッケージ（コンパイラやDBクライアント等）をインストール
RUN apt-get update -qq && \
    apt-get install --no-install-recommends -y build-essential curl libpq-dev git libvips node-gyp pkg-config python-is-python3

# JS（資産コンパイル）に必要なNode.jsとYarnをダウンロードしてインストール
ARG NODE_VERSION=20.14.0
ARG YARN_VERSION=1.22.22
ENV PATH=/usr/local/node/bin:$PATH
RUN curl -sL https://github.com/nodenv/node-build/archive/master.tar.gz | tar xz -C /tmp/ && \
    /tmp/node-build-master/bin/node-build "${NODE_VERSION}" /usr/local/node && \
    npm install -g yarn@$YARN_VERSION && \
    rm -rf /tmp/node-build-master

# Gemfileをコピーし、本番用のGemをインストール（キャッシュ等は即削除して軽量化）
COPY Gemfile Gemfile.lock ./
RUN bundle install && \
    rm -rf ~/.bundle/ "${BUNDLE_PATH}"/ruby/*/cache "${BUNDLE_PATH}"/ruby/*/bundler/gems/*/.git && \
    bundle exec bootsnap precompile --gemfile

# package.jsonをコピーし、JavaScriptのパッケージ（Nodeモジュール）をインストール
COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile

# アプリケーションの全ソースコードをコンテナ内にコピー
COPY . .

# アプリ起動を高速化するため、Bootsnapのキャッシュを事前生成
RUN bundle exec bootsnap precompile app/ lib/

# 本番環境用のWeb資産（CSS/JS等）をコンパイル（ダミーキーを使いマスターキー不要で実行）
RUN SECRET_KEY_BASE_DUMMY=1 ./bin/rails assets:precompile


# --- 【ステージ2：実行用】実際に本番サーバーとして動く最終イメージ ---
FROM base

# 本番環境の動作に最低限必要なパッケージ（PostgreSQLクライアント等）だけをインストール
RUN apt-get update -qq && \
    apt-get install --no-install-recommends -y curl default-postgresql-client libvips && \
    rm -rf /var/lib/apt/lists /var/cache/apt/archives

# ★超重要：上でビルドした「Gems」と「アプリ（アセット含む）」の成果物だけをクリーンにコピー
COPY --from=build /usr/local/bundle /usr/local/bundle
COPY --from=build /rails /rails

# セキュリティ対策：root（管理者）ではなく、権限を絞った「rails」ユーザーを作成して実行権限を与える
RUN useradd rails --create-home --shell /bin/bash && \
    chown -R rails:rails db log storage tmp
USER rails:rails

# 起動直前にデータベースの準備（マイグレーション等）を行うスクリプトを指定
ENTRYPOINT ["/rails/bin/docker-entrypoint"]

# コンテナが外部と通信するポート番号（3000番）を指定
EXPOSE 3000
# デフォルトの起動コマンド（Railsサーバーを起動）
CMD ["./bin/rails", "server"]
```
::::



::::details compose.yml
```yml
services:
  # --- 1. データベース（PostgreSQL）の設定 ---
  db:
    image: postgres:16 # 公式のPostgreSQL 16のイメージを使用
    environment:
      POSTGRES_PASSWORD: myapp_password # データベースのパスワードを設定
      TZ: Asia/Tokyo                    # コンテナ内のタイムゾーンを日本時間に設定
    volumes:
      - postgres_data:/var/lib/postgresql/data # コンテナを消してもデータが消えないように永続化
    ports:
      - 5432:5432 # パソコン側の5432番ポートを、コンテナの5432番（標準）に接続
    # データベースが正常に起動したかを自動チェックする設定（ヘルスチェック）
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"] # 接続可能か確認するコマンドを実行
      interval: 10s      # 10秒ごとにチェック
      timeout: 10s       # 10秒以上応答がなければタイムアウト
      retries: 3         # 3回連続失敗で「異常（unhealthy）」とみなす
      start_period: 30s  # 起動直後の30秒間は、失敗しても大目に見る

  # --- 2. Railsアプリケーション（Webサーバー）の設定 ---
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev # 開発用の「Dockerfile.dev」を基にコンテナをビルド
    # コンテナ起動時に自動実行するコマンド一式
    # 1. Gemを同期 2. DB作成・マイグレーション 3. 古いサーバー起動ファイルを削除 4. 開発用サーバー起動
    command: bash -c "bundle install && bundle exec rails db:prepare && rm -f tmp/pids/server.pid && ./bin/dev"
    tty: true          # コンテナ内のログに色をつけたり、コマンドを入力できるようにする
    stdin_open: true   # `binding.pry` 等でのデバッグ時、キーボード入力を受け付けるようにする
    volumes:
      - .:/myapp                                 # 手元のソースコードの変更をリアルタイムにコンテナへ反映（同期）
      - bundle_data:/usr/local/bundle:cached     # インストールしたGemを保持して、2回目以降の起動を高速化
      - node_modules:/myapp/node_modules         # JSパッケージの管理データをコンテナ内に保持
    environment:
      TZ: Asia/Tokyo # タイムゾーンを日本時間に設定
    ports:
      - "3000:3000" # パソコンのブラウザ（localhost:3000）からコンテナ内のRailsにアクセス可能にする
    # 起動の順番をコントロールする設定
    depends_on:
      db:
        condition: service_healthy # dbが「単に起動した」だけでなく「ヘルスチェックで正常（healthy）」になったらwebを起動

# --- データの保存場所（ボリューム）の定義 ---
volumes:
  postgres_data: # DBのデータ用
  bundle_data:   # RubyのGem用
  node_modules:  # JavaScriptのライブラリ用
  ```
::::

::::deitaisl config/routes.rb
  ```rb
Rails.application.routes.draw do
  # 1. ActionCableの設定
  mount ActionCable.server => "/cable"

  # 2. ルートパス
  root "comments#index"

  # 3. ユーザー認証（Devise）
  devise_for :users, controllers: {
    omniauth_callbacks: "users/omniauth_callbacks",
    passwords: "users/passwords",
    registrations: "users/registrations"
  }

  # 4. 静的ページ
  get "/terms", to: "pages#terms"
  get "/privacy", to: "pages#privacy"

  # 5. 通常のリソース管理（CRUD）
  resources :comments
  resources :users

  # 6. 音楽・アーティスト関連（重複を整理）
  resources :artists do
    resources :musics, only: [ :index, :show ]
  end
  resources :musics, only: [ :index, :show ]

  # 7. ライブ・チャット・イベント関連
  resources :live_events, only: [ :index, :show, :new, :create ] do
    resources :musics, only: [ :index, :show ] # イベントに関連する楽曲
  end

  resources :live_rooms do
    resources :messages, only: [ :create ]
  end

  # ライブ詳細(show)も resources 内で適切に管理
  resources :lives, only: [ :create, :new, :show ] do
    resources :rooms, only: [ :show ]
    resource :chat_rooms, only: [] do
      resources :chat_messages, only: [ :create ]
    end
  end

  # 単一のチャットルーム
  get "chat", to: "rooms#show"
end
  ```
  ::::


  ::::details deploy_sample/config/application.rb

  ```rb
  require_relative "boot" # アプリの起動準備（ライブラリのパス設定など）を行う boot.rb を読み込みます

require "rails/all" # Railsの全標準機能（ActiveRecord, ActionController, ActiveStorage等）を丸ごと読み込みます

# Gemfileに書かれた各種ライブラリ（Gem）を一括で読み込みます。
# 本番・開発・テストの各環境（Rails.groups）に応じたGemだけが適切にロードされます。
Bundler.require(*Rails.groups)

module Myapp
  class Application < Rails::Application
    # Rails 7.1のデフォルト設定（セキュリティやパフォーマンスの推奨値）をベースとして一括適用します。
    config.load_defaults 7.1
    
    # アプリケーションの言語設定を「日本語（:ja）」にします。これにより、エラーメッセージなどが日本語化されます。
    config.i18n.default_locale = :ja
    
    # アプリ内の時刻（データベースの保存時や時間の計算時）の基準を「日本時間（Tokyo）」に設定します。
    config.time_zone = "Tokyo"

    # lib ディレクトリに関する自動読み込み（オートロード）の設定です。
    # Rubyファイルではない「assets（画像やCSS等）」や「tasks（バッチ処理等）」を読み込み対象から除外（ignore）し、無駄なエラーを防ぎます。
    config.autoload_lib(ignore: %w[assets tasks])
    
    # Active Storage（画像保存機能）で画像のリサイズやトリミングを行う際、
    # 従来のImageMagickではなく、処理が非常に高速で軽量な「libvips（:vips）」というエンジンを使用する設定です。
    config.active_storage.variant_processor = :vips

    # アプリケーション共通の設定はここに追記していきます。
    # ※特定の環境（本番だけ、開発だけ）の設定は、config/environments/ 内の各ファイルで上書きします。
    #
    # config.time_zone = "Central Time (US & Canada)"
    # config.eager_load_paths << Rails.root.join("extras")
  end
end
```
::::

::::details app/channels/live_room_channel.rb
```rb
class LiveRoomChannel < ApplicationCable::Channel
  # ユーザーがチャットルームに接続（参加）した瞬間に実行されるメソッド
  def subscribed
    # フロントエンド（JavaScript）から送られてきたルームIDを元に、該当するルームを検索
    live_room = LiveRoom.find(params[:live_room_id])
    
    # このユーザーと特定のチャットルームを結びつけ、配信のストリーム（通り道）を開始します
    stream_for live_room
  end

  # フロントエンドからメッセージや画像が送られてきた（発言した）時に実行されるメソッド
  def speak(data)
    # 送られてきたデータ（dataハッシュ）から、ルームID、画像データ、メッセージ内容を取り出す
    live_room = LiveRoom.find_by(id: data["live_room_id"])
    image_data = data["image"]
    message_content = data["message"].presence || "" # 空文字やnilの場合は空の文字列にする

    # 取得した情報から、新しいメッセージオブジェクト（下書き）を作成
    message = live_room.messages.new(content: message_content, user_id: data["user_id"])

    # もし画像データ（Base64形式のテキストデータ）が含まれている場合の処理
    if image_data.present?
      # テキスト形式の画像データを、コンピュータが理解できるバイナリ（データ）に復元
      decoded_image = Base64.decode64(image_data)
      
      # Active Storageを使って、復元したデータを「uploaded_image.png」としてメッセージに添付
      message.image.attach(io: StringIO.new(decoded_image), filename: "uploaded_image.png")
    end

    # メッセージ（と画像）をデータベースに強制保存（失敗したら例外エラー）
    message.save!
    
    # 【ここで伏線回収！】
    # 通常のコントローラー外（ActionCable内）なので、ApplicationControllerの特殊メソッドを使い、
    # メッセージを投稿したユーザーとして「ログインしている状態」を模倣して部分テンプレート（HTML）を組み立てます。
    template = ApplicationController.render_with_signed_in_user(
      User.find(data["user_id"]),
      partial: "messages/message",
      locals: { message: message }
    )
    
    # 組み立てたHTMLテンプレートを、このチャットルームに参加している全員の画面へリアルタイムに一斉配信（キャスト）します
    LiveRoomChannel.broadcast_to(live_room, template)
  end
end
```
::::


#### 削除する？
::::削除するファイル
```app/controllers/comments_controller.rb
class CommentsController < ApplicationController
  def index
  end
end
```
```deploy_sample/db/seeds.rb
# This file should ensure the existence of records required to run the application in every environment (production,
# development, test). The code here should be idempotent so that it can be executed at any point in every environment.
# The data can then be loaded with the bin/rails db:seed command (or created alongside the database with db:setup).
#
# Example:
#
#   ["Action", "Comedy", "Drama", "Horror"].each do |genre_name|
#     MovieGenre.find_or_create_by!(name: genre_name)
#   end
```

```app/assets/config/manifest.js
//= link_tree ../images
//= link_tree ../builds
```





::::




・app/channels/application_cableのファイルは、
app/channels/application_cable/channel.rb
```
module ApplicationCable
  class Channel < ActionCable::Channel::Base
  end
end
```
app/channels/application_cable/connection.rb
```
module ApplicationCable
  class Connection < ActionCable::Connection::Base
  end
end
```
があるが、これは不要？必要？