---
title: "tailwindについて"
emoji: "🦁"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: [tailwind]
published: false
---


# 🌀 Tailwind CSS まとめ

## 1. Tailwind CSSとは

- ユーティリティクラスベースのCSSフレームワーク  
- CSSを直接書かず、HTML上でクラス名を組み合わせてデザインを作成  
- コンポーネント設計やレスポンシブ対応が簡単  
- 他のCSSフレームワーク（Bootstrapなど）に比べて柔軟性が高い  

---

## 2. Tailwindの導入方法

### 📘 CDNで読み込む方法（学習・検証用）

```html
<script src="https://cdn.tailwindcss.com"></script>
```

npmでインストール（開発用）

```
npm install -D tailwindcss
npx tailwindcss init
```

設定ファイル（tailwind.config.js）でテーマをカスタマイズ
```
// tailwind.config.js
theme: {
  extend: {
    colors: {
      brand: '#1da1f2',
    },
    fontFamily: {
      sans: ['Inter', 'sans-serif'],
    },
  },
},
```


基本的な書き方

クラス名をHTMLタグに直接指定します。

```
<button class="bg-blue-500 text-white px-4 py-2 rounded-lg">送信</button>
```

4. よく使うユーティリティクラス

レイアウト系：flex, grid, justify-center, items-center, gap-4

余白系：p-4（padding）, m-2（margin）

色系：bg-blue-500, text-gray-700, border-red-300

テキスト系：text-xl, font-bold, text-center

角丸・影：rounded-lg, shadow-md

サイズ指定：w-full, h-12, max-w-md


5. レスポンシブデザイン

Tailwindではブレークポイントプレフィックスを使用します。
```
<div class="text-sm md:text-lg lg:text-xl">レスポンシブテキスト</div>
```


デフォルトブレークポイント一覧
| プレフィックス | 最小幅     |
| ------- | ------- |
| `sm`    | 640px〜  |
| `md`    | 768px〜  |
| `lg`    | 1024px〜 |
| `xl`    | 1280px〜 |

6. カスタマイズとテーマ設定
tailwind.config.jsでテーマカラーやフォントを上書きできます。
```
theme: {
  extend: {
    colors: {
      brand: '#1da1f2',
    },
    fontFamily: {
      sans: ['Inter', 'sans-serif'],
    },
  },
},
```

7. プラグインの活用

- Forms：@tailwindcss/forms
- Typography（Prose）：@tailwindcss/typography
- Aspect Ratio：@tailwindcss/aspect-ratio

例：
```
npm install @tailwindcss/forms
```

8. 実践例：カードデザイン
```
<div class="max-w-sm bg-white rounded-2xl shadow-lg p-4">
  <img src="/image.jpg" alt="example" class="rounded-xl mb-3" />
  <h2 class="text-lg font-bold">カードタイトル</h2>
  <p class="text-gray-600 text-sm mt-1">ここに説明文を記載します。</p>
  <button class="mt-3 bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600">詳細を見る</button>
</div>
```

9. よくあるエラー・ハマりポイント
クラスが反映されない場合
→ content 配列に対象HTML/ERBファイルのパスが含まれていない
```
content: ["./app/**/*.html.erb", "./app/helpers/**/*.rb", "./app/javascript/**/*.js"]
```

10. Tailwindを使う上でのコツ
一度に多くのクラスを使わず、徐々にデザインを組み立てる
共通レイアウトは@applyディレクティブでまとめる
```
/* example.css */
.btn {
  @apply bg-blue-500 text-white px-4 py-2 rounded-lg;
}
```
DaisyUIなどの拡張ライブラリを併用すると効率UP



11. 便利な拡張ライブラリ
DaisyUI
TailwindベースのUIコンポーネント集
```
npm install daisyui
```

```
plugins: [require("daisyui")]
```
その他人気ライブラリ
- Flowbite
- Headless UI
- Tailwind Elements